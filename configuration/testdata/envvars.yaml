debug: false
server:
  port: 8080
  images: /static
scrape:
  enabled: true
  polling: 15m
  collection: 1h
database:
  host: ${pg_host}
  port: ${pg_port}
  database: ${pg_database}
  username: ${pg_user}
  password: ${pg_password}
tado:
  username: tado
  password: ${tado_password}
solarEdge:
  token: 1234